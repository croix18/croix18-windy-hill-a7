# A7 3.05  Applying Exponent Laws — rational bases with negative exponents.
L = dict(
    code="3.05", unit=3, lesson_no=5, title="Applying Exponent Laws",
    benchmark="MA.8.NSO.1.3",
    benchmark_text="Extend previous understanding of the laws of exponents to include integer exponents. Apply the laws of exponents to evaluate numerical expressions and generate equivalent numerical expressions, limited to integer exponents and rational-number bases, with procedural fluency.",
    target="I can rewrite a rational base with a negative exponent using a positive exponent, and apply several laws to generate an equivalent expression.",
    yesterday="Yesterday: a negative exponent means reciprocal.",
    today="Today the base is a fraction — so the reciprocal flips it.",
    essential="How do you rewrite a rational base with a negative exponent as an equivalent expression with a positive exponent?",
    building_on="3.04 — the negative exponent law; 3.02 — power of a quotient; Grade 6 — reciprocals of fractions.",
    working_toward="3.06–3.07 — evaluating and testing equivalence with everything at once; Thread A — the same laws with variable bases.",
    vocab=[("reciprocal of a fraction", "swap numerator and denominator: the reciprocal of $\\frac{2}{5}$ is $\\frac{5}{2}$; the sign stays where it was"),
           ("rational base with a negative exponent", "$\\left(\\frac{a}{b}\\right)^{-n} = \\left(\\frac{b}{a}\\right)^{n}$ — flip the base, make the exponent positive")],
    ixl=["Identify equivalent expressions involving exponents I — VLM"],

    warmup=[
        dict(stem="Find the value:  $2^{-3}$", answer="$\\frac{1}{8}$", band="yesterday", source="3.04 — negative exponent law", check=("eq", "F(2)**-3", "F(1,8)")),
        dict(stem="Rewrite with a positive exponent:  $\\frac{1}{7^{-2}}$", answer="$7^{2}$", band="earlier this week", source="3.04 — the law's second form", check=("eq", "1/F(7)**-2", "49")),
        dict(stem="Which is larger, $4^{3}$ or $3^{4}$?", answer="$3^{4}$ (81 > 64)", band="earlier this unit", source="3.01 — evaluating powers", check=("true", "3**4 > 4**3")),
        dict(stem="Write the reciprocal of  $-\\frac{2}{5}$", answer="$-\\frac{5}{2}$", band="prior grade — the prerequisite this lesson needs", source="Grade 6 — reciprocals of fractions (MA.6.NSO.2): flip the fraction, keep the sign", check=("eq", "F(-2,5)*F(-5,2)", "1")),
    ],
    warmup_note="Q4 is today's move: a negative fraction's reciprocal is negative. A board with 5/2 (sign dropped) or 2/5 (opposite of the opposite) is the error this lesson is built to catch — the B1G-M's first task for this benchmark — explain the difference between −b and b⁻¹ — is aimed at exactly this.",

    notes=[
        dict(numeral="I", head="The pattern with a fraction base", min=4, sub="Copy the table.",
             note="Each row is the previous row divided by 2/5, which is multiplying by 5/2. Going down from (2/5)⁰ = 1: 1 · 5/2 = 5/2, then 25/4, then 125/8. Students see that (2/5)⁻¹ is 5/2 — the reciprocal — before the rule is stated.\nOFF: 'To divide by two fifths you multiply by five halves. That is why the base flips.'",
             table=([2.3, 2.6, 2.6], [["Expression", "Expanded", "Value"], ["(2/5)²", "2/5 · 2/5", "4/25"], ["(2/5)¹", "2/5", "2/5"], ["(2/5)⁰", "1", "1"], ["(2/5)⁻¹", "5/2", "5/2"], ["(2/5)⁻²", "5/2 · 5/2", "25/4"]]),
             letters=False, tsize=17),
        dict(numeral="II", head="Flip the base, make the exponent positive", min=4, sub="",
             note="State the rule and then the reason: (a/b)⁻ⁿ = 1/(a/b)ⁿ = 1 ÷ aⁿ/bⁿ = bⁿ/aⁿ = (b/a)ⁿ. Then the sign question: (−2/3)⁻⁵ is (−3/2)⁵ — the minus rides along with the fraction. A student who writes (2/3)⁵ took the opposite AND the reciprocal — the −b vs b⁻¹ confusion the guide's task targets.\nOFF: 'Reciprocal flips top and bottom. It does not touch the sign.'",
             math=["$\\left(\\frac{a}{b}\\right)^{-n} = \\left(\\frac{b}{a}\\right)^{n}$          ($a, b \\ne 0$)",
                   "$\\left(\\frac{2}{5}\\right)^{-2} = \\left(\\frac{5}{2}\\right)^{2} = \\frac{25}{4}$          $\\left(-\\frac{2}{3}\\right)^{-5} = \\left(-\\frac{3}{2}\\right)^{5}$"],
             items2=["**The sign stays with the fraction. Flip the numbers, not the sign.**",
                     "A whole number is a fraction over 1:  $4^{-3} = \\left(\\frac{4}{1}\\right)^{-3} = \\left(\\frac{1}{4}\\right)^{3}$"],
             letters=False),
        dict(numeral="III", head="Several laws at once — the plan is still yours", min=3, sub="",
             note="Fernanda: quotient of powers first, then power of a power, then rewrite the decimal as a fraction and flip. Junior: power of a quotient first, then quotient. Both land on (2/5)². The decimal-to-fraction step is the one to watch: 2.5 = 5/2, so (2.5)⁻² = (5/2)⁻² = (2/5)².",
             math=["$\\left(\\frac{2.5^{5}}{2.5^{3}}\\right)^{-1} = \\left(2.5^{2}\\right)^{-1} = 2.5^{-2} = \\left(\\frac{5}{2}\\right)^{-2} = \\left(\\frac{2}{5}\\right)^{2} = \\frac{4}{25}$"],
             items2=["Any correct order of laws gives the same value. Finish with a positive exponent."],
             letters=False),
    ],

    examples=[
        dict(title="Example 1", sub="Flip, then evaluate.", min_q=1,
             note_q="SPOKEN ONLY. What is the base? What does the negative exponent tell you to do to it? Do the numbers or the sign flip?",
             prompt=["$\\left(-\\frac{7}{8}\\right)^{-2}$"], ask="Rewrite with a positive exponent, then find the value.",
             worked=[dict(sub="Worked.", min=2,
                          note="Flip: (−8/7)². Square: 64/49. Even power, so positive. A board with −64/49 squared the digits but kept the minus, reading (−8/7)² as −(8/7)²; a board with 49/64 never flipped.",
                          rows=[("\\left(-\\frac{7}{8}\\right)^{-2} = \\left(-\\frac{8}{7}\\right)^{2}", "flip the base; the sign stays"),
                                ("\\left(-\\frac{8}{7}\\right)^{2} = \\frac{64}{49}", "even power: positive")],
                          answer="64/49")],
             check=("eq", "F(-7,8)**-2", "F(64,49)"),
             your_turn=dict(min=2, note="Catches a student who flips the sign with the fraction (the answer here is negative, and a board with +8/125 dropped it) and one who never flipped (−125/8). −8/125 is right.",
                            prompt=["$\\left(-\\frac{5}{2}\\right)^{-3}$"], gloss="(−2/5)³ — odd power, negative", answer_latex="-\\frac{8}{125}"),
             yt_check=("eq", "F(-5,2)**-3", "F(-8,125)")),
        dict(title="Example 2", sub="Two laws, then a flip.", min_q=1,
             note_q="SPOKEN ONLY. Same base twice — which law joins them? What is the exponent after that, and what does its sign tell you?",
             prompt=["$\\left(\\frac{5}{8}\\right)^{-2}\\cdot\\left(\\frac{5}{8}\\right)^{4}$"], ask="Write an equivalent expression with one base and a positive exponent.",
             worked=[dict(sub="Worked.", min=2,
                          note="Product of powers: −2 + 4 = 2. The exponent is already positive, so no flip. Value 25/64. The trap is flipping anyway because a negative exponent appeared somewhere in the problem.",
                          rows=[("\\left(\\frac{5}{8}\\right)^{-2}\\cdot\\left(\\frac{5}{8}\\right)^{4} = \\left(\\frac{5}{8}\\right)^{-2+4} = \\left(\\frac{5}{8}\\right)^{2}", "product of powers"),
                                ("\\left(\\frac{5}{8}\\right)^{2} = \\frac{25}{64}", "already positive: no flip")],
                          answer="(5/8)² = 25/64")],
             check=("eq", "F(5,8)**-2*F(5,8)**4", "F(25,64)"),
             your_turn=dict(min=2, note="Catches a student who adds to −1 and stops there (a negative exponent is not a finished answer), and one who writes (−5/4)¹⁰ — the exponents' signs dropped, 7 + 3, so the base never flipped. (−4/5)¹⁰ is right.",
                            prompt=["$\\left(-\\frac{5}{4}\\right)^{-7}\\cdot\\left(-\\frac{5}{4}\\right)^{-3}$"], gloss="exponent −10, then flip: (−4/5)¹⁰", answer_latex="\\left(-\\frac{4}{5}\\right)^{10}"),
             yt_check=("eq", "F(-5,4)**-7*F(-5,4)**-3", "F(-4,5)**10")),
    ],

    whiteboard=[
        dict(text=["A recipe is scaled by a factor of  $\\left(\\frac{2}{3}\\right)^{-4}$.", "The original recipe serves 6 people.", "Rewrite the factor with a positive exponent."],
             qtext="A recipe is scaled by a factor of (2/3)⁻⁴. The original recipe serves 6 people. Rewrite the factor with a positive exponent.",
             unneeded="number of people it serves", hint="Rewrite with a positive exponent.", gloss="flip the base", answer_latex="\\left(\\frac{3}{2}\\right)^{4}",
             note="Ruling 22's board: the serving size is there to be read past — the question asks only for the factor. A board with (2/3)⁴ dropped the minus without flipping; a board with −(3/2)⁴ made the exponent's sign the value's sign.", check=("eq", "F(2,3)**-4", "F(3,2)**4"),
             wrong="(2/3)⁴ — the minus was dropped without flipping [8.NSO.1.3 negative exponent law]; −(3/2)⁴ — negative exponent read as a negative value [B1G-M 8.NSO.1.3 task: −b vs b⁻¹]; anything built from 6 — the unneeded figure was used"),
        dict(latex="\\left(\\frac{3}{-5}\\right)^{-9}", hint="Rewrite with a positive exponent.", gloss="flip the numbers; the sign stays in the fraction", answer_latex="\\left(\\frac{-5}{3}\\right)^{9}",
             note="Sign inside the denominator. A board with (5/3)⁹ took the opposite as well as the reciprocal — the −b vs b⁻¹ confusion the guide's task targets.", check=("eq", "F(3,-5)**-9", "F(-5,3)**9"),
             wrong="(5/3)⁹ — took the opposite as well as the reciprocal [B1G-M 8.NSO.1.3 task: −b vs b⁻¹]"),
        dict(latex="\\left(-\\frac{8}{9}\\right)^{-2}", hint="Find the value.", gloss="(−9/8)² = 81/64", answer_latex="\\frac{81}{64}",
             note="Flip then square. A board with −81/64 kept a sign through an even power; 64/81 never flipped.", check=("eq", "F(-8,9)**-2", "F(81,64)"),
             wrong="−81/64 — sign kept through an even power; 64/81 — never flipped [8.NSO.1.3 negative exponent law]"),
        dict(latex="\\left(\\frac{21}{4}\\right)^{-5}", hint="Rewrite with a positive exponent.", gloss="flip", answer_latex="\\left(\\frac{4}{21}\\right)^{5}",
             note="Bigger numbers, same move. A board that tried to evaluate wasted the time: rewrite was asked.", check=("eq", "F(21,4)**-5", "F(4,21)**5"),
             wrong="(21/4)⁵ — minus dropped without flipping; 4/21⁵ — flipped but applied the exponent to the denominator only"),
        dict(kind="mc", latex="\\left(-\\frac{7}{8}\\right)^{-3}", text=["Which expression is equivalent?"], choices=["(−8/7)³", "(8/7)³", "(−7/8)³", "(7/8)³"], correct=0, answer="A — (−8/7)³",
             errors={"B": "took the opposite as well as the reciprocal [B1G-M 8.NSO.1.3 task: −b vs b⁻¹]",
                     "C": "dropped the minus on the exponent without flipping [negative exponent law not applied]",
                     "D": "took the opposite of the base instead of its reciprocal [B1G-M 8.NSO.1.3 task: −b vs b⁻¹]"},
             note="Diagnostic, and the odd power is the point. Example 1's even power hid the sign of the base: (−8/7)² and (8/7)² are both 64/49. Here the cube keeps the sign, so the four options are four different numbers — A is −512/343 and B is +512/343. A board with B flipped the base and dropped its minus; ask what the reciprocal of −7/8 is. C never flipped; D took the opposite instead of the reciprocal.",
             check=("many", ("eq", "F(-7,8)**-3", "F(-8,7)**3"), ("true", "F(8,7)**3 != F(-7,8)**-3"), ("true", "F(-7,8)**3 != F(-7,8)**-3"), ("true", "F(7,8)**3 != F(-7,8)**-3"))),
        dict(kind="mc", latex="\\left(\\frac{5}{8}\\right)^{-2}\\div\\left(\\frac{5}{8}\\right)^{-4}", text=["Which expression is equivalent?"], choices=["(5/8)²", "(8/5)²", "(5/8)⁻⁶", "(5/8)⁸"], correct=0, answer="A — (5/8)²",
             errors={"B": "flipped a base whose exponent was already positive [negative exponent law applied where no negative exponent remained]",
                     "C": "added −2 + (−4) instead of subtracting [quotient law: subtract the exponents]",
                     "D": "multiplied −2 · −4 [confused with power of a power]"},
             note="Diagnostic. −2 − (−4) = 2. The B board is the interesting one: it did the arithmetic right and then flipped out of habit.",
             check=("eq", "F(5,8)**-2/F(5,8)**-4", "F(5,8)**2")),
        dict(latex="\\left(\\frac{2}{5}\\right)^{-1}\\cdot\\left(\\frac{5}{2}\\right)^{1}", hint="Find the value.", gloss="(5/2)¹ · (5/2)¹ = (5/2)²", answer_latex="\\frac{25}{4}",
             note="Two bases that are reciprocals of each other: flip the first and they match. A board with 1 multiplied 2/5 by 5/2 and forgot the exponent flipped the first factor.", check=("eq", "F(2,5)**-1*F(5,2)**1", "F(25,4)"),
             wrong="1 — treated (2/5)⁻¹ as 2/5 and multiplied by its reciprocal [negative exponent law not applied]; 5/2 — only one factor counted"),
        dict(latex="\\left[\\left(\\frac{2}{3}\\right)^{5}\\cdot\\left(\\frac{2}{3}\\right)^{-2}\\right]^{3}", hint="Write it with one base and one positive exponent.", gloss="(2/3)³ inside, then power of a power", answer_latex="\\left(\\frac{2}{3}\\right)^{9}",
             note="Product then power of a power: (5 + (−2)) · 3 = 9. A board with (2/3)²¹ dropped the minus and used (5 + 2) · 3; (2/3)⁶ added 3 + 3.", check=("eq", "(F(2,3)**5*F(2,3)**-2)**3", "F(2,3)**9"),
             wrong="(2/3)⁶ — added 3 + 3 in the power-of-a-power step [confused with product of powers]; (3/2)⁹ — flipped a positive exponent"),
        dict(kind="written", text=["Marco says  $\\left(-\\frac{2}{3}\\right)^{-5}$  can be rewritten as  $\\left(\\frac{2}{3}\\right)^{5}$", "because 'two negatives make a positive.'  Is he right?"], qtext="Marco says (−2/3)⁻⁵ can be rewritten as (2/3)⁵ because 'two negatives make a positive.' Is he right?",
             hint="Say what the negative exponent does, and what it does not do. Give the correct rewrite.", gloss="the exponent flips the fraction; it never touches the sign", answer="No — (−2/3)⁻⁵ = (−3/2)⁵",
             note="Written. This is the confusion the state guide's task for this benchmark targets (−b vs b⁻¹), in the student's own words. Full credit: the correct rewrite and a sentence that separates reciprocal from opposite. Value, if anyone asks: −243/32.",
             check=("many", ("eq", "F(-2,3)**-5", "F(-3,2)**5"), ("true", "F(-2,3)**-5 != F(2,3)**5")),
             wrong="'Yes' — opposite taken as well as reciprocal [B1G-M 8.NSO.1.3 task: −b vs b⁻¹]"),
    ],

    bank=[
        dict(heading="For questions 1 through 4, rewrite each expression as an equivalent expression with a positive exponent."),
        dict(stem="$\\left(\\frac{3}{4}\\right)^{-3}$", answer="$\\left(\\frac{4}{3}\\right)^{3}$", why="", check=("eq", "F(3,4)**-3", "F(4,3)**3"), space=0.5),
        dict(stem="$\\left(-\\frac{7}{8}\\right)^{-2}$", answer="$\\left(-\\frac{8}{7}\\right)^{2}$", why="The sign stays with the fraction.", check=("eq", "F(-7,8)**-2", "F(-8,7)**2"), space=0.5),
        dict(stem="$\\left(\\frac{-4}{5}\\right)^{-4}$", answer="$\\left(\\frac{5}{-4}\\right)^{4}$", why="", check=("eq", "F(-4,5)**-4", "F(5,-4)**4"), space=0.5),
        dict(stem="$\\left(\\frac{22}{7}\\right)^{-7}$", answer="$\\left(\\frac{7}{22}\\right)^{7}$", why="", check=("eq", "F(22,7)**-7", "F(7,22)**7"), space=0.5),
        dict(heading="Matching."),
        dict(stem="Match each expression on the left with the equivalent expression on the right by writing the letter.", parts=[
            dict(label="a", stem="$\\left(-\\frac{11}{10}\\right)^{-3}$        A. $\\left(-\\frac{11}{10}\\right)^{3}$      B. $\\left(\\frac{10}{11}\\right)^{3}$      C. $\\left(\\frac{11}{10}\\right)^{3}$      D. $\\left(-\\frac{10}{11}\\right)^{3}$", answer="D", why="", check=("eq", "F(-11,10)**-3", "F(-10,11)**3"), space=0.3),
            dict(label="b", stem="$\\left(\\frac{11}{10}\\right)^{-3}$", answer="B", why="", check=("eq", "F(11,10)**-3", "F(10,11)**3"), space=0.3),
            dict(label="c", stem="$\\left(-\\frac{10}{11}\\right)^{-3}$", answer="A", why="", check=("eq", "F(-10,11)**-3", "F(-11,10)**3"), space=0.3),
            dict(label="d", stem="$\\left(\\frac{10}{11}\\right)^{-3}$", answer="C", why="", check=("eq", "F(10,11)**-3", "F(11,10)**3"), space=0.3)]),
        dict(heading="For questions 6 through 9, generate an equivalent expression with one base and one positive exponent. Show the laws you use."),
        dict(stem="$\\left(-\\frac{14}{17}\\right)^{-2}\\cdot\\left(-\\frac{14}{17}\\right)^{-4}$", answer="$\\left(-\\frac{14}{17}\\right)^{-6} = \\left(-\\frac{17}{14}\\right)^{6}$", why="Product of powers, then flip.", check=("eq", "F(-14,17)**-2*F(-14,17)**-4", "F(-17,14)**6"), space=0.9),
        dict(stem="$\\left(\\frac{5}{8}\\right)^{-2}\\div\\left(\\frac{5}{8}\\right)^{-4}$", answer="$\\left(\\frac{5}{8}\\right)^{2}$", why="−2 − (−4) = 2; already positive.", check=("eq", "F(5,8)**-2/F(5,8)**-4", "F(5,8)**2"), space=0.9),
        dict(stem="$\\left[\\left(\\frac{2}{3}\\right)^{5}\\cdot\\left(\\frac{2}{3}\\right)^{-2}\\right]^{3}$", answer="$\\left(\\frac{2}{3}\\right)^{9}$", why="(5 − 2) · 3.", check=("eq", "(F(2,3)**5*F(2,3)**-2)**3", "F(2,3)**9"), space=0.9),
        dict(stem="$\\left(\\frac{2.5^{5}}{2.5^{3}}\\right)^{-1}$", answer="$2.5^{-2} = \\left(\\frac{5}{2}\\right)^{-2} = \\left(\\frac{2}{5}\\right)^{2}$", why="Rewrite the decimal as a fraction before flipping.", check=("eq", "(F(5,2)**5/F(5,2)**3)**-1", "F(2,5)**2"), space=0.9),
        dict(heading="Equivalent or not."),
        dict(stem="Consider the expression  $\\frac{\\left(\\frac{1}{6}\\right)^{3}}{6^{-5}}\\cdot 6^{4}$.  For each expression below, decide whether it is equivalent, and justify with a law of exponents.", parts=[
            dict(label="a", stem="$\\frac{6^{-3}}{6^{-5}}\\cdot 6^{4}$", answer="Yes — $\\left(\\frac{1}{6}\\right)^{3} = 6^{-3}$, so this is the same expression; value $6^{6}$.", why="", check=("eq", "F(1,6)**3/F(6)**-5*F(6)**4", "6**6"), space=0.6),
            dict(label="b", stem="$6^{-8}\\cdot 6^{4}$", answer="No — the quotient gives $6^{2}$, not $6^{-8}$; this is $6^{-4}$.", why="", check=("true", "F(6)**-8*F(6)**4 != 6**6"), space=0.6),
            dict(label="c", stem="$\\left(\\frac{1}{6}\\right)^{-2}\\cdot 6^{4}$", answer="Yes — $\\left(\\frac{1}{6}\\right)^{-2} = 6^{2}$, and $6^{2}\\cdot 6^{4} = 6^{6}$.", why="", check=("eq", "F(1,6)**-2*F(6)**4", "6**6"), space=0.6),
            dict(label="d", stem="$6^{6}$", answer="Yes — it is the value of the expression written as one power.", why="", check=("eq", "F(1,6)**3/F(6)**-5*F(6)**4", "6**6"), space=0.5)]),
        dict(stem="Which is the value of  $\\left(-\\frac{2}{3}\\right)^{-3}$ ?", choices=["$-\\frac{27}{8}$", "$\\frac{27}{8}$", "$-\\frac{8}{27}$", "$\\frac{8}{27}$"], correct=0, answer="A",
             errors={"B": "took the opposite as well as the reciprocal [B1G-M 8.NSO.1.3 task: −b vs b⁻¹]", "C": "never flipped the base [negative exponent law not applied]", "D": "never flipped, and dropped the sign [both errors]"},
             why="(−3/2)³ = −27/8.", check=("eq", "F(-2,3)**-3", "F(-27,8)")),
        dict(stem="Find the value of $n$ that makes each statement true.", parts=[
            dict(label="a", stem="$\\left(\\frac{3}{5}\\right)^{n} = \\frac{25}{9}$", answer="n = −2", why="(5/3)² = 25/9.", check=("eq", "F(3,5)**-2", "F(25,9)"), space=0.6),
            dict(label="b", stem="$\\left(\\frac{1}{2}\\right)^{n} = 8$", answer="n = −3", why="", check=("eq", "F(1,2)**-3", "8"), space=0.6)]),
    ],

    additional=[
        dict(heading="For questions 1 through 4, rewrite each expression as an equivalent expression with a positive exponent."),
        dict(stem="$\\left(-\\frac{4}{9}\\right)^{-5}$", answer="$\\left(-\\frac{9}{4}\\right)^{5}$", why="", check=("eq", "F(-4,9)**-5", "F(-9,4)**5"), space=0.5),
        dict(stem="$\\left(\\frac{9}{10}\\right)^{-6}$", answer="$\\left(\\frac{10}{9}\\right)^{6}$", why="", check=("eq", "F(9,10)**-6", "F(10,9)**6"), space=0.5),
        dict(stem="$\\left(\\frac{-1}{2}\\right)^{-8}$", answer="$(-2)^{8}$", why="The reciprocal of −1/2 is −2.", check=("eq", "F(-1,2)**-8", "(-2)**8"), space=0.5),
        dict(stem="$\\left(\\frac{2}{7}\\right)^{-3}$", answer="$\\left(\\frac{7}{2}\\right)^{3}$", why="", check=("eq", "F(2,7)**-3", "F(7,2)**3"), space=0.5),
        dict(heading="Matching."),
        dict(stem="Match each expression on the left with the equivalent expression on the right by writing the letter.", parts=[
            dict(label="a", stem="$\\left(-\\frac{13}{9}\\right)^{-3}$        A. $\\left(\\frac{13}{9}\\right)^{3}$      B. $\\left(-\\frac{13}{9}\\right)^{3}$      C. $\\left(-\\frac{9}{13}\\right)^{3}$      D. $\\left(\\frac{9}{13}\\right)^{3}$", answer="C", why="", check=("eq", "F(-13,9)**-3", "F(-9,13)**3"), space=0.3),
            dict(label="b", stem="$\\left(\\frac{13}{9}\\right)^{-3}$", answer="D", why="", check=("eq", "F(13,9)**-3", "F(9,13)**3"), space=0.3),
            dict(label="c", stem="$\\left(-\\frac{9}{13}\\right)^{-3}$", answer="B", why="", check=("eq", "F(-9,13)**-3", "F(-13,9)**3"), space=0.3),
            dict(label="d", stem="$\\left(\\frac{9}{13}\\right)^{-3}$", answer="A", why="", check=("eq", "F(9,13)**-3", "F(13,9)**3"), space=0.3)]),
        dict(heading="For questions 6 through 9, generate an equivalent expression with one base and one positive exponent. Show the laws you use."),
        dict(stem="$\\left(-\\frac{5}{4}\\right)^{-7}\\cdot\\left(-\\frac{5}{4}\\right)^{-3}$", answer="$\\left(-\\frac{5}{4}\\right)^{-10} = \\left(-\\frac{4}{5}\\right)^{10}$", why="", check=("eq", "F(-5,4)**-7*F(-5,4)**-3", "F(-4,5)**10"), space=0.9),
        dict(stem="$\\left(\\frac{3}{7}\\right)^{-5}\\div\\left(\\frac{3}{7}\\right)^{-2}$", answer="$\\left(\\frac{3}{7}\\right)^{-3} = \\left(\\frac{7}{3}\\right)^{3}$", why="−5 − (−2) = −3, then flip.", check=("eq", "F(3,7)**-5/F(3,7)**-2", "F(7,3)**3"), space=0.9),
        dict(stem="$\\left[\\left(\\frac{4}{5}\\right)^{-2}\\right]^{3}\\cdot\\left(\\frac{4}{5}\\right)^{8}$", answer="$\\left(\\frac{4}{5}\\right)^{-6}\\cdot\\left(\\frac{4}{5}\\right)^{8} = \\left(\\frac{4}{5}\\right)^{2}$", why="", check=("eq", "(F(4,5)**-2)**3*F(4,5)**8", "F(4,5)**2"), space=0.9),
        dict(stem="$\\left(\\frac{1.5^{4}}{1.5^{2}}\\right)^{-1}$", answer="$1.5^{-2} = \\left(\\frac{3}{2}\\right)^{-2} = \\left(\\frac{2}{3}\\right)^{2}$", why="", check=("eq", "(F(3,2)**4/F(3,2)**2)**-1", "F(2,3)**2"), space=0.9),
        dict(heading="Equivalent or not."),
        dict(stem="Consider the expression  $\\frac{\\left(\\frac{1}{4}\\right)^{3}}{4^{-5}}\\cdot 4^{4}$.  For each expression below, decide whether it is equivalent, and justify with a law of exponents.", parts=[
            dict(label="a", stem="$\\frac{4^{5}\\cdot 4^{4}}{4^{3}}$", answer="Yes — both simplify to $4^{6}$.", why="", check=("eq", "F(4)**5*F(4)**4/F(4)**3", "4**6"), space=0.6),
            dict(label="b", stem="$4^{-8}\\cdot 4^{4}$", answer="No — this is $4^{-4}$.", why="", check=("true", "F(4)**-8*F(4)**4 != 4**6"), space=0.6),
            dict(label="c", stem="$\\left(\\frac{1}{4}\\right)^{-2}\\cdot 4^{4}$", answer="Yes — $4^{2}\\cdot 4^{4} = 4^{6}$.", why="", check=("eq", "F(1,4)**-2*F(4)**4", "4**6"), space=0.6),
            dict(label="d", stem="$4^{6}$", answer="Yes.", why="", check=("eq", "F(1,4)**3/F(4)**-5*F(4)**4", "4**6"), space=0.5)]),
        dict(stem="Which is the value of  $\\left(-\\frac{3}{10}\\right)^{-3}$ ?", choices=["$-\\frac{1000}{27}$", "$\\frac{1000}{27}$", "$-\\frac{27}{1000}$", "$\\frac{27}{1000}$"], correct=0, answer="A",
             errors={"B": "took the opposite as well as the reciprocal [B1G-M 8.NSO.1.3 task: −b vs b⁻¹]", "C": "never flipped the base [negative exponent law not applied]", "D": "never flipped, and dropped the sign [both errors]"},
             why="", check=("eq", "F(-3,10)**-3", "F(-1000,27)")),
        dict(stem="Find the value of $n$ that makes each statement true.", parts=[
            dict(label="a", stem="$\\left(\\frac{2}{7}\\right)^{n} = \\frac{49}{4}$", answer="n = −2", why="", check=("eq", "F(2,7)**-2", "F(49,4)"), space=0.6),
            dict(label="b", stem="$\\left(\\frac{1}{3}\\right)^{n} = 27$", answer="n = −3", why="", check=("eq", "F(1,3)**-3", "27"), space=0.6)]),
    ],

    mtr=[("MTR.5.1", "Notes I — 3.04's halving table redone with a fraction base, so the flip appears as the next line of a pattern the class already owns rather than a second rule."),
         ("MTR.4.1", "Board 5 — when the room splits between (−8/7)³ and (8/7)³, the sixty-second re-vote before the reveal: is the reciprocal of −7/8 positive or negative?"),
         ("MTR.6.1", "Board 3 — a base between −1 and 0 raised to a negative even power; students say which side of 1 the value lands on before computing it.")],

    hoq=[("Marco says two negatives make a positive, so (−2/3)⁻⁵ is (2/3)⁵. Which negative is he cancelling, and what does each of the two actually do?", "DOK 3"),
         ("4⁻³ and (1/4)³ are the same number. Write 4 as a fraction and show why they have to be.", "DOK 2"),
         ("For which bases is (a/b)⁻ⁿ larger than 1, and for which is it smaller? Give an example of each.", "DOK 3")],

    differentiation=dict(
        ese="Give the rule as one picture — the fraction with a curved arrow turning it over and the exponent's minus crossed out — and let the student redraw it at the top of the page. On the first four boards the only job is the flip; the arithmetic comes after.",
        ell="Flip is the word. Say 'flip the fraction, drop the minus' on every board, and keep 2/3 → 3/2 written up as the picture of what flip means.",
        enrichment="Ask for 0.4⁻² by converting the decimal to a fraction first, and for the n that makes (2/3)ⁿ = 81/16."),

    closure="Board 9 is written work and it is the exit evidence: the student has to say separately what the base's sign does and what the exponent's sign does. Read the boards, not the papers.",

    independent=[
        dict(stem="Rewrite with a positive exponent.  $\\left(\\frac{3}{4}\\right)^{-2}$", answer="$\\left(\\frac{4}{3}\\right)^{2}$", why="", check=("eq", "F(3,4)**-2", "F(4,3)**2"), space=0.8),
        dict(stem="Find the value.  $\\left(\\frac{2}{5}\\right)^{-2}$", answer="$\\frac{25}{4}$", why="Flip, then square.", check=("eq", "F(2,5)**-2", "F(25,4)"), space=0.8),
        dict(stem="Rewrite with a positive exponent.  $\\left(-\\frac{5}{6}\\right)^{-3}$", answer="$\\left(-\\frac{6}{5}\\right)^{3}$", why="The base keeps its sign through the flip.", check=("eq", "F(-5,6)**-3", "F(-6,5)**3"), space=0.8),
        dict(stem="Find the value.  $\\left(\\frac{1}{2}\\right)^{-4}$", answer="16", why="(2/1)⁴.", check=("eq", "F(1,2)**-4", "16"), space=0.8),
        dict(stem="Find the value.  $\\left(\\frac{3}{2}\\right)^{-1}\\cdot\\left(\\frac{3}{2}\\right)^{3}$", answer="$\\left(\\frac{3}{2}\\right)^{2} = \\frac{9}{4}$", why="−1 + 3 = 2.", check=("eq", "F(3,2)**-1*F(3,2)**3", "F(9,4)"), space=0.8),
        dict(stem="Find the value of $n$.  $\\left(\\frac{4}{7}\\right)^{n} = \\left(\\frac{7}{4}\\right)^{3}$", answer="n = −3", why="Flipping the base flips the exponent's sign.", check=("eq", "F(4,7)**-3", "F(7,4)**3"), space=0.8),
    ],

    te=dict(
        say=["A negative exponent flips the base. On a fraction, that means turn it over.",
             "The base keeps its own sign through the flip. Negative two-thirds flips to negative three-halves, not three-halves.",
             "A whole number is a fraction over 1, so 4⁻³ and (1/4)³ are one rule, not two."],
        must="Rational-number bases with integer exponents: rewrite with positive exponents, and apply several laws to generate an equivalent expression.",
        must_not="No fractional exponents. Bases are nonzero, and the exponent's sign never moves the base's sign.",
        read_first=[
            "This lesson rebuilds Math Nation 3.5 (SE pp. 124–129). The book's guided instruction (the (2/5) pattern table) is Notes I; its Fernanda/Junior collaboration is Notes III; its equivalence table and matching activity are bank questions 5 and 10. Every value on the book's pages checks.",
            "Whiteboard 5 was first built on the book's (−7/8)⁻², where (8/7)² has the same value as the key (64/49) — a student who chose it was right and was marked wrong. It now uses (−7/8)⁻³: the odd power keeps the sign, so every option is a different number and the discussion about the reciprocal's sign still happens.",
            "The B1G-M task for this benchmark — the difference between −b and b⁻¹; here, (−2/3)⁻⁵ misread as (2/3)⁵ — is warm-up 4, Notes II, whiteboard 2, 5 and 9, and bank question 11."],
        standard_notes=[("Clarification", "Rational-number bases with integer exponents; fractions and decimals both appear, and a decimal base is converted to a fraction before it is flipped."),
                        ("Boundary", "A whole-number base is a fraction over 1, so 4⁻³ = (1/4)³ — said on Notes II so that 3.04 and 3.05 are one rule, not two.")],
        sits="Second MA.8.NSO.1.3 lesson. 3.06+07 uses everything from 3.01–3.05 in one expression.",
        lives="Notes II — 'flip the numbers, not the sign'. Whiteboard 9 asks for it in writing. If the round shows B on question 5, ask for the reciprocal of −7/8 out loud — the sign is the whole question.",
        materials="Whiteboards and markers. Calculators are allowed on everything; (−7/8)^(−2) on a calculator returns 1.306..., which is 64/49 — worth converting once so students trust both forms.",
        variation="Questions 1–4 are one move each and vary only where the sign sits: no sign, a sign on the fraction, a sign in the numerator, a bigger fraction with no sign. Question 5 puts four sign placements side by side. 6–9 add one law before the flip (product, quotient, power of a power, then a decimal that must become a fraction). The pattern breaks at 7, where the exponent comes out positive and there is nothing to flip, and at 10, where one of four candidate expressions is not equivalent. The Additional sheet mirrors each position with different numbers.",
        audit=["Math Nation 3.5 pages verified: the (2/5) table, (3/2)⁴, (−5/3)⁹, (−9/8)², (4/21)⁵, (2/5)², the 6⁶ equivalence table, the matching arrows, (2/3)⁹, (2/−13)³, 5⁸, (4/−5)¹⁰.",
               "Practice L5's header says 'questions 6–10' over a table that is 7–10; not carried over.",
               "The book's warm-up (60⁶³ vs 63⁶⁰, no calculation) is a fine number talk but retrieves nothing; the comparison 3⁴ vs 4³ takes its place in the warm-up's third band.",
               "Practice L5 and Homework L5 keys check; their matching items are bank question 5 in both banks."],
        changes=["3.5.1 number-talk warm-up → four retrieval questions.", "3.5.2 Guided Instruction → Notes I–II.", "3.5.3 Collaboration (Fernanda/Junior) → Notes III.", "3.5.4 Collaboration (equivalence table) → bank question 10.", "3.5.5 Your Turn and 3.5.6 Ticket Out the Door → the round."],
    ),
)
